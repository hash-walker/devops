# Frontend Dockerfile - Builds the React application
FROM node:20

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source files needed for building React app
COPY public ./public
COPY src ./src

# We'll build at runtime so the output goes to the shared volume
# The build command will be run in docker-compose

